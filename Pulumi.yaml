name: evercharge-sftp
description: An SFTP server which will be used by OCPP chargers. Primarily for pulling firmware files and uploading diagnostics.
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: aws-typescript
